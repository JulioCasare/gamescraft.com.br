# Quem abre o menu agora e o plugin GcMenus, que faz o bau de verdade. O
# datapack continua dono dos bonecos — corpo, placa do nome e caixa de clique —
# e o plugin le a marca gcnpc_<jogo> dessas pecas para saber qual menu abrir.
#
# A abertura por dialogo que morava aqui foi tirada: com o plugin instalado,
# abriam os dois menus no mesmo clique.
