# Generated into the config directory of every Paper backend.
# Velocity modern forwarding prevents direct backend connections from impersonating
# players when the shared secret is kept private.

proxies:
  bungee-cord:
    online-mode: true
  proxy-protocol: false
  velocity:
    enabled: true
    online-mode: true
    secret: "__VELOCITY_SECRET__"
